/**
 * Barrel for the dashboard's metric grid. Import via `@ui/metric-grid`; the grid
 * is data-agnostic and renders the preformatted {@link Metric}s it is given.
 */

export { MetricGrid } from "./metric-grid";
export type { Metric } from "./metric-grid";
