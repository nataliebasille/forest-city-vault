/**
 * Barrel for the portal's inline stroke icons. Import individual icons from
 * `@ui/icons`; each icon inherits `currentColor` and takes an optional
 * `className` for sizing.
 */

export { CloseIcon } from "./close-icon";
export type { IconProps } from "./icon-props";
export { MenuIcon } from "./menu-icon";
export { SignOutIcon } from "./sign-out-icon";
