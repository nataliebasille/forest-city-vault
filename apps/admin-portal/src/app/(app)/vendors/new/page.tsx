/**
 * The `/vendors/new` route. The add slide-over is rendered by the vendors
 * {@link import("../vendor-panel-host").VendorPanelHost} (which reacts to this
 * URL), so this segment only needs to exist for `/vendors/new` to resolve.
 */
export default function NewVendorPage() {
  return null;
}
