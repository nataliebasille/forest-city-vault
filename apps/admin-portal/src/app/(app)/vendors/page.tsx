/**
 * The `/vendors` index. The roster itself is rendered by the vendors
 * {@link import("./layout").default layout}; this route adds nothing over it, so
 * with no sub-route open the page shows just the list (no slide-over).
 */
export default function VendorsPage() {
  return null;
}
